"""Deliberately rule-breaking module."""
import json
from datetime import datetime


class legacy_holder:
    """A badly named legacy class."""


def DoTheThing(x):
    """Do the thing the old way."""
    return x
