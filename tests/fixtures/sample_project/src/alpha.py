"""Alpha module."""
import json
import os
from typing import Optional


class AlphaClient:
    """Talks to the alpha service."""

    def fetch_data(self, key: str) -> Optional[dict]:
        """Fetch data for a key."""
        return None


def load_alpha(path: str) -> dict:
    """Load alpha data from a path."""
    return {}
